# AGENTS.md

## Cursor Cloud specific instructions

This repository ("workflow") is currently an empty scaffold with only a `README.md`.

- **No application code, dependencies, or services exist yet.** There is nothing to build, lint, test, or run.
- **No package manager lockfile or dependency manifest is present.** When application code is added, update the VM setup script accordingly (e.g., `npm install`, `pip install -r requirements.txt`, etc.).
- **Git** is the only tool required at this stage.

When the first real code is committed, update this section with:
1. How to install dependencies and run the dev server.
2. How to run linting and tests.
3. Any non-obvious caveats discovered during setup.
